import { EmailPatternDirective } from './email-pattern.directive';

describe('EmailPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailPatternDirective();
    expect(directive).toBeTruthy();
  });
});
